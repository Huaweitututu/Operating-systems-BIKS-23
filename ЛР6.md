# Cловник базових англійських термінів (Видиборець Назар)

1. **Compression** – Reducing file size using algorithms.
2. **Archiving** – Combining files into a single archive.
3. **Lossless Compression** – Compression that fully restores the original file.
4. **Lossy Compression** – Compression with permanent data loss (e.g., MP3, JPEG).
5. **gzip** – Fast compression tool using DEFLATE.
   - `-r` – Compresses recursively.
   - `-l` – Shows file stats.
   - `-c` – Outputs to stdout.
   - `-d` – Decompresses.
   - `-1 to -9` – Compression level (1=fast, 9=thorough).
6. **bzip2** – Better compression using Burrows-Wheeler, slower than gzip.
   - `-s` – Low-memory mode.
   - `-d` – Decompresses.
   - `-1 to -9` – Block size (memory vs. compression).
7. **xz** – Modern tool with LZMA2, best compression, high resource use.
   - `-l` – Shows stats.
   - `-c` – Outputs to stdout.
   - `-d` – Decompresses.
   - `-0 to -9` – Compression level.
   - `-e` – Extreme compression mode.
8. **tar** – Archiving tool, often paired with compression.
   - `-c` – Creates archive.
   - `-t` – Lists contents.
   - `-x` – Extracts.
   - `-v` – Verbose output.
   - `-f` – Specifies file name.
   - `-z` – Uses gzip.
   - `-j` – Uses bzip2.
   - `-J` – Uses xz.
9. **Algorithm** – Method for compressing data.
10. **Dictionary** – Set of substitutions for repeated data.
11. **DEFLATE** – Algorithm used by gzip.
12. **Burrows-Wheeler** – Algorithm used by bzip2.
13. **LZMA2** – Algorithm used by xz.
14. **File Extension** – Suffix indicating format (e.g., `.gz`, `.bz2`, `.xz`).
15. **Recursive** – Processes all files in directories and subdirectories.

Ось перефразований текст у Markdown-форматі з збереженням обсягу та структури:

---

# Огляд команд Linux

## Команди стиснення та архівації

1. **`tar`** – Створення та розпакування архівів.

   * Опції: `-c` (створити), `-x` (витягти), `-t` (переглянути), `-v` (детально), `-f` (файл), `-z` (gzip), `-j` (bzip2), `-J` (xz).
   * Встановлення: `sudo apt install tar`

2. **`xz`** – Потужне стиснення за допомогою LZMA2.

   * Опції: `-d` (розпакувати), `-c` (вивести у stdout), `-l` (статистика), `-0` до `-9` (рівень), `-e` (екстремальне стиснення).
   * Встановлення: `sudo apt install xz-utils`

3. **`zip`** – Універсальне кросплатформне стиснення.

   * Опції: `-r` (рекурсивно), `-d` (видалити), `-u` (оновити), `-9` (макс. стиснення).
   * Встановлення: `sudo apt install zip unzip`

4. **`bzip2`** – Ефективне, але повільніше за gzip стиснення.

   * Опції: `-d` (розпакування), `-s` (менше пам’яті), `-1` до `-9` (розмір блоку).
   * Встановлення: `sudo apt install bzip2`

5. **`gzip`** – Швидке стиснення за алгоритмом DEFLATE.

   * Опції: `-d` (розпакувати), `-r` (рекурсивно), `-c` (вивід у stdout), `-1` до `-9` (рівень).
   * Встановлення: `sudo apt install gzip`

### Приклади

1. `tar -czvf archive.tar.gz /path/dir` – Створення архіву з gzip.
2. `xz -9 file.txt` – Стиснення `file.txt` у `file.txt.xz`.
3. `zip -r archive.zip /path/dir` – Запакування директорії у zip.

---

## Команди перегляду файлів

1. **`cat`** – Виводить або об’єднує вміст файлів.

   * Опції: `-n` (нумерувати рядки), `-b` (нумерувати лише непорожні), `>` (перенаправлення).
   * Встановлення: `sudo apt install coreutils`

2. **`less`** – Погодинний перегляд із прокруткою.

   * Опції: `-N` (номер рядків), `-S` (без переносу рядків).
   * Встановлення: `sudo apt install coreutils`

3. **`more`** – Пошаговий перегляд (тільки вперед).

   * Опції: `-d` (підказки), `+n` (почати з n-рядка).
   * Встановлення: `sudo apt install coreutils`

4. **`head`** – Виводить початок файлу (за замовчуванням: 10 рядків).

   * Опції: `-n N` (показати N рядків), `-c N` (N байт).
   * Встановлення: `sudo apt install coreutils`

5. **`tail`** – Виводить останні рядки (за замовчуванням: 10 рядків).

   * Опції: `-n N` (показати N рядків), `-f` (слідкувати за оновленням).
   * Встановлення: `sudo apt install coreutils`

---

## Конвеєри, потоки та фільтри Shell

* **Потоки**: `stdin` (0), `stdout` (1), `stderr` (2).
* **Конвеєри (`|`)**: Передають `stdout` в `stdin` наступної команди (напр., `ls | grep "txt"`).
* **Фільтри**: Обробляють потоки даних (напр., `grep`, `sort`).
* **Перенаправлення**: `>` (перезапис), `>>` (додавання), `<` (ввід).
* Можна об'єднувати прості команди в складні операції.

---

## Команда `grep`

* **Призначення**: Пошук шаблонів у тексті або файлах.
* **Приклади**: `grep "error" log.txt`, `ls | grep "txt"`.
* **Опції**: `-i` (ігнорувати регістр), `-r` (рекурсивно), `-n` (номери рядків), `-v` (виключити збіги).

---


---

## Shell Pipes, Streams, and Filters

- **Streams**: `stdin` (0), `stdout` (1), `stderr` (2).  
- **Pipes (`|`)**: Send `stdout` to `stdin` (e.g., `ls | grep "txt"`).  
- **Filters**: Process data (e.g., `grep`, `sort`).  
- **Redirects**: `>` (overwrite), `>>` (append), `<` (input).  
- Combines simple commands for complex tasks.

---

## `grep` Command

- **Purpose**: Searches for patterns in files/input.  
- **Examples**: `grep "error" log.txt`, `ls | grep "txt"`.  
- **Options**: `-i` (case-insensitive), `-r` (recursive), `-n` (line numbers), `-v` (invert match).


# Хід роботи (Видиборець Назар)

### Таблиця-опис команд

| Команда | Опис |
|---------|------|
| `mkdir mybackups` | Створення нової директорії `mybackups` у домашньому каталозі користувача. |
| `tar -cvf mybackups/udev.tar /etc/udev` | Команда `tar` використовується для об’єднання кількох файлів в один файл. В даному випадку вміст директорії `/etc/udev` буде збережено в архів `udev.tar` у директорії `mybackups`. Параметр `-c` повідомляє команді `tar` створити файл tar. Параметр `-v` означає "verbose", який наказує команді `tar` продемонструвати, що вона робить. Параметр `-f` використовується для вказівки назви файлу tar. |
| `gzip 'назва файлу'` | Виконує стиснення поточного файлу, змінюючи його формат на `.gz`, використовує алгоритм стиснення Lempel-Ziv. |
| `gzip -l 'назва файлу'` | Виводить інформацію про стиснення поточного файлу. |
| `gunzip 'назва стиснутого файлу'` | Декомпресує файл, тобто повертає його до первинного стану. |
| `gzip -d` | Те саме, що й `gunzip`. |
| `bzip` | Виконує стиснення за алгоритмом "Burrows-Wheeler block sorting", змінює формат на `.bz`. |
| `bunzip` | Декомпресує файл формату `.bz`. |
| `xz` | Виконує стиснення за алгоритмом "Lempel-Ziv-Markov (LZMA)", змінює формат на `.xz`. |
| `unxz` | Декомпресує файл формату `.xz`. |
| `tar -c [-f ARCHIVE] [OPTIONS] [FILE...]` | Створення архіву. Замість `ARCHIVE` може бути будь-яка назва для архіву. Замість `FILE` повинна бути назва файлу чи файлів, які будуть заархівовані. |
| `tar -cf alpha_files.tar alpha*` | Приклад минулої команди. Всі файли, що починаються на `alpha`, будуть включені до архіву `alpha_files.tar`. |
| `tar -czf alpha_files.tar.gz alpha*` | З параметром `-z` виконує стиснення архіву за допомогою команди `gzip`. |
| `tar -cjf folders.tbz School` | Разом з параметром `-j` виконує стиснення за допомогою команди `bzip2`. |
| `tar -tjf folders.tbz` | Параметр `-t` використовується для виведення списку файлів в архіві, а `-j` в даному випадку декомпресує файли за допомогою команди `bunzip2`. |
| `tar -xjf folders.tbz` | Параметр `-x` використовується для розархівування файлів. |
| `tar -xjvf folders.tbz` | Параметр `-v` виводить почергово виводить на екран файли, що розархівовуються. |
| `tar -xjvf folders.tbz School/Art/linux.txt` | Додаючи в кінці назву файлу (`School/Art/linux.txt`), лише цей файл буде розархівований. |
| `zip alpha_files.zip alpha*` | Архівує файли в формат `.zip`. Також після виконання показує на скільки файли були стиснуті у %. |
| `zip -r School.zip School` | Те ж, що й попередня команда, але з рекурсією. |
| `unzip -l School.zip` | Виводить список файлів в архіві. |
| `unzip School.zip` | Виконує розархівування файлів, до кожного файлу дає певну опцію розархівування. Файли розархівовуються в поточний каталог, тому щоб уникнути незручностей з опціями можна скопіювати архів в новий каталог та розархівувати його там. |
| `unzip School.zip School/Math/numbers.txt` | Розархівовує файл, який прописаний в кінці. |
| `cat food.txt` | Виводить вміст текстового файлу `food.txt`. |
| `less` | Виводить зміст файлу посторінково з можливістю переміщення по змісту. |
| `more` | Виводить зміст файлу посторінково з можливістю переміщення лише вперед. |
| `head /etc/sysctl.conf` | Виводить 10 перших рядків з файлу. |
| `tail -5 /etc/sysctl.conf` | Виводить останні 5 рядків з файлу. |
| `head -n 3 /etc/sysctl.conf` | Виводить перші 3 рядки. |
| `tail -n +25` | Відображає вміст файлу з 25-го рядка і до кінця. |
| `nl` | Номерує те, що виводить команда, наприклад, `ls`. |
| `sort mypasswd` | Сортує зміст файлу `mypasswd` за алфавітним порядком. |
| `sort -t: -n -k3 mypasswd` | Сортує зміст файлу. `-t` відповідає за розмежування, `-k` відповідає на номер поля сортування (в нашому випадку це третє, бо `-k3`), `-n` відповідає за тип сортування. |
| `wc /etc/passwd /etc/passwd-` | Виводить статистику файлу: кількість байт, слів, рядків та назву файлу. |
| `cut -d: -f1,5-7 mypasswd` | Виводить колонки з файлу. `-d` відповідає за розділення виведених даних за допомогою табуляції, `-f` визначає які поля виводити. |
| `grep --color bash /etc/passwd` | Фільтрує вміст файлу та виводить його чи результат іншої команди. Параметр `--color` відповідає за підсвічення результатів сортування (в нашому випадку будуть підсвічуватись всі слова `bash` у виводі). |
| `grep -c bash /etc/passwd` | Виводить кількість підходящих рядків. |
| `grep -n bash /etc/passwd` | Виводить колонки з файлу та підписує їх номер в файлі. |
| `grep -v nologin /etc/passwd` | За допомогою `-v` виведе всі рядки, що не включають слово (в нашому випадку `nologin`). |
| `grep '....' red.txt` | Виведе всі слова, що містять 4 символи з текстового файлу (`.` відповідає за 1 символ). |
| `grep '[^0-9]' profile.txt` | Виведе всі рядки, що включають числа 0-9 і підсвітить всі символи, крім чисел. |
| `grep 're*d' red.txt` | Виведе рядки, в яких слово починається на `re` і закінчується на `d` (`*` відповідає за кількість символів від 0 до нескінченності). |
| `grep '^root' /etc/passwd` | Виведе рядки, які починаються словом `root`. |
| `grep 'r$' alpha-first.txt` | Виведе рядки, які закінчуються буквою `r`. |
| `grep 're\*' newhome.txt` | Виведе рядки, в яких є саме частинка `re`. |
| `grep -E 'e+' red.txt` | Виведе рядки, в яких є один та більше символів `е`. |
| `grep -E 'gray\|grey' spelling.txt` | Виведе рядки, в яких є слова `gray` або `grey`. |

![Image](https://github.com/user-attachments/assets/2f711148-ab1f-47fd-8188-67a57f75a1d8)

### Порівняльна характеристика процесів стискання та архівування

**Стискання**:
- **Мета**: Зменшити розмір файлу шляхом видалення надлишкових даних.
- **Результат**: Один або кілька стиснених файлів.
- **Приклади форматів**: `.zip`, `.gz`, `.bz2`, `.xz`.

**Архівування**:
- **Мета**: Об'єднати кілька файлів або каталогів у один файл для зручності зберігання та передачі.
- **Результат**: Один архівний файл, який може містити стиснуті або нестиснуті дані.
- **Приклади форматів**: `.tar`, `.zip`, `.7z`.

### Програми для стискання та архівування в Linux

| Програма | Формат | Опис |
|----------|--------|------|
| **gzip** | `.gz`  | Використовує алгоритм DEFLATE. Швидкий, але менш ефективний для дуже великих файлів. |
| **bzip2** | `.bz2` | Використовує алгоритм Burrows-Wheeler. Краще стискання, ніж у gzip, але повільніший. |
| **xz** | `.xz` | Використовує алгоритм LZMA. Найефективніший для стискання, але найповільніший. |
| **zip** | `.zip` | Використовує алгоритм DEFLATE. Підтримує архівування та стискання. |
| **7z** | `.7z` | Використовує різні алгоритми, включаючи LZMA. Високе стискання, але повільний. |

### Порівняння алгоритмів стискання

| Алгоритм    | Швидкість | Ефективність | Використання |
|-------------|-----------|--------------|--------------|
| **DEFLATE** | Висока    | Середня      | gzip, zip    |
| **BZIP2**   | Середня   | Висока       | bzip2        |
| **LZMA**    | Низька    | Дуже висока  | xz, 7z       |

- **Найшвидший**: DEFLATE (використовується в gzip та zip).
- **Найефективніший**: LZMA (використовується в xz та 7z).

### Програмні засоби для стискання та архівування на мобільних телефонах

| Програма       | Опис                                                                 | Переваги                                      |
|----------------|----------------------------------------------------------------------|-----------------------------------------------|
| **ZArchiver**  | Додаток для Android, який підтримує різні формати архівів.           | Простий у використанні, підтримує багато форматів. |
| **RAR**        | Офіційний додаток для роботи з архівами `.rar` та `.zip` на Android. | Висока швидкість та ефективність.             |
| **B1 Archiver**| Додаток для Android та iOS, який підтримує більшість популярних форматів архівів. | Безкоштовний, підтримує багато форматів.     |
| **WinZip**     | Додаток для Android та iOS, який дозволяє стискати та розпаковувати файли. | Інтеграція з хмарними сервісами, зручний інтерфейс. |

Ці програми забезпечують зручність роботи з архівами на мобільних пристроях, дозволяючи стискати та розпаковувати файли прямо на телефоні.

### Перенаправлення потоків виведення в bash

| Команда               | Що виконує команда?                                                                 |
|-----------------------|------------------------------------------------------------------------------------|
| `cmd 1> file`         | Перенаправляє стандартний вивід (stdout) команди `cmd` у файл `file`.               |
| `cmd > file`          | Те саме, що й `cmd 1> file`. Перенаправляє стандартний вивід у файл `file`.          |
| `cmd 2> file`         | Перенаправляє стандартний помилковий вивід (stderr) команди `cmd` у файл `file`.     |
| `cmd >> file`         | Додає стандартний вивід команди `cmd` у кінець файлу `file`.                         |
| `cmd &> file`         | Перенаправляє і стандартний вивід, і стандартний помилковий вивід у файл `file`.     |
| `cmd > file 2>&1`     | Перенаправляє стандартний вивід у файл `file`, а потім перенаправляє stderr туди ж.  |
| `cmd >> file 2>&1`    | Додає стандартний вивід у кінець файлу `file` і перенаправляє stderr туди ж.         |
| `cmd 2>&1 > /dev/null`| Перенаправляє stderr туди ж, куди й stdout (який відкидається у `/dev/null`).        |
| `cmd 2>/dev/null`     | Перенаправляє stderr у `/dev/null`, тобто відкидає помилки.                         |
| `cmd1 | cmd2`         | Перенаправляє stdout команди `cmd1` на вхід команди `cmd2`.                         |
| `cmd1 2>&1 | cmd2`    | Перенаправляє і stdout, і stderr команди `cmd1` на вхід команди `cmd2`.              |



### Приклади команд та їх перенаправлення

| Команда                                      | Що виконує команда?                                                                 | Який потік перенаправлення? |
|----------------------------------------------|------------------------------------------------------------------------------------|-----------------------------|
| `$ echo "It is a new story." > story`        | Записує рядок "It is a new story." у файл `story`.                                  | stdout                      |
| `$ date > date.txt`                          | Записує поточну дату та час у файл `date.txt`.                                       | stdout                      |
| `$ cat file1 file2 file3 > bigfile`          | Об'єднує вміст файлів `file1`, `file2`, і `file3` у файл `bigfile`.                 | stdout                      |
| `$ ls -l >> directory`                       | Додає детальний список файлів у поточній директорії у кінець файлу `directory`.      | stdout                      |
| `$ sort < file1_unsorted > file2_sorted`     | Сортує вміст файлу `file1_unsorted` і записує результат у файл `file2_sorted`.       | stdin та stdout             |
| `$ find -name '* txt' > file.txt 2>/dev/null`| Шукає файли, що відповідають шаблону `* txt`, і записує результат у `file.txt`.      | stdout та stderr            |
| `$ cat file1_unsorted | sort > file2_sorted` | Читає вміст файлу `file1_unsorted`, сортує його і записує результат у `file2_sorted`.| Конвеєр та stdout           |
| `$ cat myfile | grep student | wc -l`       | Читає вміст файлу `myfile`, шукає рядки з "student" і підраховує їх кількість.       | Конвеєр                     |



# Контрольні запитання (Видиборець Назар)


1. **Надайте порівняльну характеристику процесам стискання та архівування.**

   **Стискання**:
   - **Мета**: Зменшити розмір файлу шляхом видалення надлишкових даних.
   - **Результат**: Один або кілька стиснених файлів.
   - **Приклади форматів**: `.zip`, `.gz`, `.bz2`, `.xz`.

   **Архівування**:
   - **Мета**: Об'єднати кілька файлів або каталогів у один файл для зручності зберігання та передачі.
   - **Результат**: Один архівний файл, який може містити стиснуті або нестиснуті дані.
   - **Приклади форматів**: `.tar`, `.zip`, `.7z`.

2. **Які програми, окрім наведених в роботі, можуть використовуватись для стискання та архівування файлів та каталогів в ОС Linux? Наведіть приклади та їх короткий опис.**

   | Програма | Формат | Опис |
   |----------|--------|------|
   | **gzip** | `.gz`  | Використовує алгоритм DEFLATE. Швидкий, але менш ефективний для дуже великих файлів. |
   | **bzip2** | `.bz2` | Використовує алгоритм Burrows-Wheeler. Краще стискання, ніж у gzip, але повільніший. |
   | **xz** | `.xz` | Використовує алгоритм LZMA. Найефективніший для стискання, але найповільніший. |
   | **zip** | `.zip` | Використовує алгоритм DEFLATE. Підтримує архівування та стискання. |
   | **7z** | `.7z` | Використовує різні алгоритми, включаючи LZMA. Високе стискання, але повільний. |

3. **Порівняйте алгоритми стискання, що використовуються в командах (програмах), використовуваних в Linux. Які з алгоритмів можна вважати найшвидшим та найефективнішим?**

   | Алгоритм    | Швидкість | Ефективність | Використання |
   |-------------|-----------|--------------|--------------|
   | **DEFLATE** | Висока    | Середня      | gzip, zip    |
   | **BZIP2**   | Середня   | Висока       | bzip2        |
   | **LZMA**    | Низька    | Дуже висока  | xz, 7z       |

   - **Найшвидший**: DEFLATE (використовується в gzip та zip).
   - **Найефективніший**: LZMA (використовується в xz та 7z).

4. **Опишіть програмні засоби для стискання та архівування, що можуть бути використані у вашому мобільному телефоні.**

   | Програма       | Опис                                                                 | Переваги                                      |
   |----------------|----------------------------------------------------------------------|-----------------------------------------------|
   | **ZArchiver**  | Додаток для Android, який підтримує різні формати архівів.           | Простий у використанні, підтримує багато форматів. |
   | **RAR**        | Офіційний додаток для роботи з архівами `.rar` та `.zip` на Android. | Висока швидкість та ефективність.             |
   | **B1 Archiver**| Додаток для Android та iOS, який підтримує більшість популярних форматів архівів. | Безкоштовний, підтримує багато форматів.     |
   | **WinZip**     | Додаток для Android та iOS, який дозволяє стискати та розпаковувати файли. | Інтеграція з хмарними сервісами, зручний інтерфейс. |

5. **Опишіть та порівняйте програмні засоби для стискання та (де)архівування даних у ОС сімейства Windows.**

   | Програма | Опис | Переваги |
   |----------|------|----------|
   | **WinRAR** | Підтримує багато форматів архівів, включаючи `.rar`, `.zip`. | Висока швидкість, підтримка шифрування. |
   | **7-Zip** | Відкрита програма, що підтримує багато форматів, включаючи `.7z`, `.zip`. | Висока ефективність стискання, безкоштовна. |
   | **WinZip** | Підтримує `.zip` та інші формати, інтеграція з хмарними сервісами. | Зручний інтерфейс, підтримка хмарних сервісів. |

6. **Поясніть яким чином стискання та архівування даних може бути використано для резервування даних. В яких ще задачах системного адміністрування воно може бути використано.**

   - **Резервування даних**: Стискання та архівування дозволяють зменшити обсяг даних, що зберігаються, і об'єднати їх у один файл для зручності перенесення та зберігання.
   - **Інші задачі**:
     - Передача великих обсягів даних через мережу.
     - Архівування лог-файлів для аналізу.
     - Створення резервних копій конфігураційних файлів.

7. **Яке призначення директорії файлу /dev/null?**

   - **/dev/null**: Це спеціальний файл у Linux, який відкидає всі дані, записані в нього, і повертає EOF (кінець файлу) при читанні. Використовується для відкидання непотрібного виводу команд.
